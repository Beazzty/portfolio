export declare function toJson(id: string): {
    code: string;
    map: null;
};
export declare function toRaw(id: string): {
    code: string;
    map: null;
};
export declare function toBox(id: string): {
    code: string;
    map: null;
};
type Options = {
    default?: 'raw' | 'json' | 'box';
};
export default function viteSvg(options?: Options): {
    name: string;
    transform(_src: any, id: string): Promise<{
        code: string;
        map: null;
    } | undefined>;
};
export {};
